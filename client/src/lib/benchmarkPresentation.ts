export const externalTimingCopy = {
  eyebrow: "Equal-area raster production",
  title: "PNG-ready median runtime",
  rasterLabel: "RASTER",
  decoderLabel: "DECODE",
  scope: "Every value includes its adapter’s image-production steps and normalization. Orbiqo’s value is not encoder-only: it includes its direct PNG renderer at 725 DPI with 2× supersampling.",
} as const;
